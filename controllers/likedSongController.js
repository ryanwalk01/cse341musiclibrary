// Placeholder controller for the liked songs page NOT IMPLEMENTED YET
module.exports = {
  getLikedSongs: (req, res) =>
    res.status(501).json({ message: "Not implemented yet" }),
  likeSong: (req, res) =>
    res.status(501).json({ message: "Not implemented yet" }),
  unlikeSong: (req, res) =>
    res.status(501).json({ message: "Not implemented yet" }),
};
