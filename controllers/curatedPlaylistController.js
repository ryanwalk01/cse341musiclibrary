// Placeholders until controllers are implemented
module.exports = {
  getCuratedPlaylists: (req, res) =>
    res.status(501).json({ message: "Not implemented yet" }),
  refreshCuratedPlaylists: (req, res) =>
    res.status(501).json({ message: "Not implemented yet" }),
};
